[package]
name = "openlock-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[features]
default = ["std"]
std = ["openlock-types/std", "openlock-crypto/std"]

[dependencies]
openlock-types = { workspace = true, default-features = false }
openlock-crypto = { workspace = true, default-features = false }
